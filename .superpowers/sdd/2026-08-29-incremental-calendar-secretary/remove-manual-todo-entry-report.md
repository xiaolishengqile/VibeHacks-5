# 移除手动待办入口报告

## 红绿证据

先将桌面端到端用例改为断言完整工作台不存在“添加待办”按钮。修复前运行：

```text
npx playwright test tests/e2e/desktop.spec.ts --grep '界面不提供手动添加待办入口'
```

失败结果：断言期望 0 个按钮，实际找到 1 个“添加待办”按钮。

移除工作台按钮及其三步对话框绑定后，使用相同命令复跑，1 项通过。

## 验证结果

- `npm run package`：通过，已重新生成桌面应用。
- `npm run test:e2e`：23 项全部通过。
- `npm test`：210 项通过，1 项既有真实代理测试跳过，0 失败。
- `npm run typecheck`：通过。
- `git diff --check`：通过。
- 检索确认渲染源码中已不存在用户可见“添加待办”入口或 `add-manual-todo` 查询；底层手动待办能力未删除。

## 提交

功能提交：`1b926e64a1f8481d5b9b22eeb27b58f14fd35916` — `移除工作台添加待办入口`
